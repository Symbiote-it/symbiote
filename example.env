# Env for agent related environment
AGENT_SERVER_URL=http://localhost:11434

# Database Configuration
# Option 1: Use full connection URL
# DATABASE_URL=postgresql://symbiote:symbiote_secret@localhost:5432/symbiote

# Option 2: Use individual components (default values shown)
DB_HOST=localhost
DB_PORT=5432
DB_USER=symbiote
DB_PASSWORD=symbiote_secret
DB_NAME=symbiote

# Optional: Enable SQL query logging (set to "true" for debugging)
DB_ECHO=false
